# Handoff

- Objective: publish the first complete version of the «Следующий клиент» landing page.
- Current state: project initialized; product design and implementation plan are canonical.
- Last verified fact: the vinext starter runs locally at `http://localhost:3000/`.
- Do not invent: contact links, client names, revenue attribution, guarantees beyond the supplied cases.
- Next action: implement `app/page.tsx`, `app/CopyCta.tsx`, and `app/globals.css`, then build and publish.
- Verification: `npx.cmd vinext build` with `WRANGLER_LOG_PATH` set in the environment.
